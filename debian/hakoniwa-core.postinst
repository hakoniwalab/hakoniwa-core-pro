#!/bin/sh
set -e
case "$1" in
  configure)
    addgroup --system hakoniwa >/dev/null 2>&1 || true

    # ディレクトリ作成と権限（setgid で中のファイルのGID固定）
    install -d -o root -g hakoniwa -m 2775 /var/lib/hakoniwa
    install -d -o root -g hakoniwa -m 2777 /var/lib/hakoniwa/mmap
  ;;
esac
exit 0
